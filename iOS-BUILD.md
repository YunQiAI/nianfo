# 念佛计数器 iOS 应用构建指南

## 项目概述
本项目已集成 Capacitor，支持将 Web 应用打包为原生 iOS 应用。

## 功能特性
- 语音识别自动计数
- 数据持久化存储
- 振动反馈
- 本地通知

## 构建步骤

### 1. 安装依赖
```bash
npm install
```

### 2. 构建 iOS 应用
```bash
./build-ios.sh
```

或手动执行：
```bash
# 更新 www 目录
rm -rf www
mkdir www
cp -r *.html *.js *.css *.jpg js www/

# 同步到 iOS 项目
npx cap sync ios

# 打开 Xcode
npx cap open ios
```

### 3. 在 Xcode 中配置
1. 选择您的开发者账号（Signing & Capabilities）
2. 选择目标设备或模拟器
3. 点击运行按钮测试应用

## 权限说明
应用需要以下权限：
- 麦克风：用于语音识别
- 语音识别：用于识别念佛声音

## 测试要点
1. 首次运行时会请求麦克风和语音识别权限
2. 点击"开始识别"按钮开始语音识别
3. 说"阿弥陀佛"等佛号进行测试
4. 检查计数是否正确增加
5. 关闭应用后重新打开，验证数据是否保存

## 故障排除
- 如果语音识别不工作，请检查设备设置中的权限
- 确保设备语言设置为中文
- 需要网络连接进行语音识别